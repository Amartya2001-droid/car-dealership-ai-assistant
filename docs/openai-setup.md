# OpenAI Setup

## Required Values
- `OPENAI_API_KEY`
- Optional: `OPENAI_MODEL`

## Mock Mode
- Keep `USE_MOCK_AI=true` for local development without external API calls.

## Production Tip
- Switch mock mode off only after verifying the deployment has valid environment variables and outbound network access.
