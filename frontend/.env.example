PORT=3001
REACT_APP_BACKEND_URL=http://localhost:3000
REACT_APP_BACKEND_PROXY_TARGET=http://localhost:3000
# Only needed once the backend sets ADMIN_API_KEY. Baked into the built JS
# bundle at build time, so this is a coarse deterrent, not real per-user auth.
REACT_APP_ADMIN_API_KEY=
